---
import Badge from '@/components/ui/Badge.astro';
import AddToCartButton from './AddToCartButton';

interface Props {
  slug: string;
  nom: string;
  photo: string;
  prix: number;
  categorie: string;
  description: string;
  disponible: boolean;
}

const { slug, nom, photo, prix, categorie, description, disponible } = Astro.props;
const unitLabel = ['volailles', 'lapin'].includes(categorie) ? '€/kg' : '€';
---

<article class={`bg-white rounded-xl overflow-hidden border border-neutral-200 shadow-sm hover:shadow-md transition-shadow group ${!disponible ? 'opacity-60' : ''}`}>
  <a href={`/boutique/${slug}`} class="block">
    <div class="aspect-[4/3] bg-neutral-100 relative overflow-hidden">
      <img src={photo} alt={nom} class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-300" loading="lazy" data-cms-field={`produits.${slug}.photo`} data-cms-type="image" />
      <div class="absolute top-3 left-3">
        <Badge variant={disponible ? 'available' : 'unavailable'}>
          {disponible ? 'Disponible' : 'Indisponible'}
        </Badge>
      </div>
    </div>
  </a>

  <div class="p-5">
    <a href={`/boutique/${slug}`}>
      <h3 class="font-heading font-bold text-neutral-900 group-hover:text-primary-700 transition-colors" data-cms-field={`produits.${slug}.nom`} data-cms-type="text">{nom}</h3>
      <p class="mt-1 text-sm text-neutral-500 line-clamp-2" data-cms-field={`produits.${slug}.description`} data-cms-type="text">{description}</p>
    </a>
    <div class="mt-4 flex items-center justify-between">
      <p class="font-heading font-bold text-xl text-accent-800" data-cms-field={`produits.${slug}.prix`} data-cms-type="number">
        {prix.toFixed(2)} <span class="text-xs font-normal text-neutral-400">{unitLabel}</span>
      </p>
      {disponible && (
        <AddToCartButton client:visible slug={slug} nom={nom} prix={prix} photo={photo} />
      )}
    </div>
  </div>
</article>
